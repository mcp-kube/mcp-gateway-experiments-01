apiVersion: v1
kind: Service
metadata:
  name: mcp-server-streamable-http
  namespace: mcp-test
  labels:
    app: mcp-server-streamable-http
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: mcp-server-streamable-http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server-streamable-http
  namespace: mcp-test
  labels:
    app: mcp-server-streamable-http
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-server-streamable-http
  template:
    metadata:
      labels:
        app: mcp-server-streamable-http
    spec:
      containers:
      - name: mcp-server
        image: aliok/mcp-server-streamable-http:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8081
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcp-server-streamable-http-route
  namespace: mcp-test
spec:
  parentRefs:
  - name: mcp-gateway
    namespace: gateway-system
  hostnames:
  - streamable-http-server.mcp.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: mcp-server-streamable-http
      port: 8081
